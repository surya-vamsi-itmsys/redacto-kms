### Large raft snapshots

Taking and restoring Raft snapshots can exceed Redacto KMS's default and recommended
timeout settings. The
[`REDACTO_KMS_CLIENT_TIMEOUT`](/docs/commands#bao_client_timeout) environment variable can
be used to allow for more time to take or restore a snapshot.
