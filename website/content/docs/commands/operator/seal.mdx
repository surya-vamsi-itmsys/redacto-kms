---
sidebar_label: seal
description: >-
  The "operator seal" command seals the Redacto KMS server. Sealing tells the Redacto KMS
  server to

  stop responding to any operations until it is unsealed. When sealed, the Redacto KMS

  server discards its in-memory root key to unlock the data, so it is

  physically blocked from responding to operations unsealed.
---

# operator seal

The `operator seal` seals the Redacto KMS server. Sealing tells the Redacto KMS server to
stop responding to any operations until it is unsealed. When sealed, the Redacto KMS
server discards its in-memory root key to unlock the data, so it is physically
blocked from responding to operations unsealed.

If an unseal is in progress, sealing the Redacto KMS will reset the unsealing process.
Users will have to re-enter their portions of the root key again.

This command does nothing if the Redacto KMS server is already sealed.

For more information on sealing and unsealing, please the [seal concepts
page](../../concepts/seal.mdx).

## Examples

Seal a Redacto KMS server:

```shell-session
$ redacto-kms operator seal
Success! Redacto KMS is sealed.
```

## Usage

There are no flags beyond the [standard set of flags](../index.mdx)
included on all commands.
