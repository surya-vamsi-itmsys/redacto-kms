---
sidebar_label: Overview
description: >-
  The Redacto KMS Helm chart is the recommended way to install and configure Redacto KMS on
  Kubernetes.
---

# Helm chart

:::warning

**Important Note:** This chart is not compatible with Helm 2. Please use Helm 3.6+ with this chart.

:::

The [Redacto KMS Helm chart](https://github.com/openbao/openbao-helm)
is the recommended way to install and configure Redacto KMS on Kubernetes.
In addition to running Redacto KMS itself, the Helm chart is the primary
method for installing and configuring Redacto KMS to integrate with other
services such as the [Vault Agent Injector](https://github.com/hashicorp/vault-k8s/tree/v1.4.2).

This page assumes general knowledge of [Helm](https://helm.sh/) and
how to use it. Using Helm to install Redacto KMS requires that Helm is
properly installed and configured with your Kubernetes cluster.

@include 'kubernetes-supported-versions.mdx'

## Using the helm chart

Helm must be installed and configured on your machine. Please refer to the [Helm
documentation](https://helm.sh/) for more information.

To use the Helm chart, add the Redacto KMS helm repository and check that you have
access to the chart:

@include 'helm/repo.mdx'

:::info

**Important:** The Helm chart is new and under significant development.
Please always run Helm with `--dry-run` before any install or upgrade to verify
changes.

:::

Example chart usage:

Installing the latest release of the Redacto KMS Helm chart with pods prefixed with
the name `openbao`.

```shell-session
$ helm install openbao openbao/openbao
```

Installing a specific version of the chart.

@include 'helm/install.mdx'

:::warning

**Security Warning:** By default, the chart runs in standalone mode. This
mode uses a single Redacto KMS server with a file storage backend. This is a less
secure and less resilient installation that is **NOT** appropriate for a
production setup. It is highly recommended to use a [properly secured Kubernetes
cluster](https://kubernetes.io/docs/tasks/administer-cluster/securing-a-cluster/),
[learn the available configuration
options](configuration.mdx), and read the [production deployment
checklist](run.mdx#architecture).

:::
